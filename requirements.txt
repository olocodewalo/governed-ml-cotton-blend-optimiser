# Core -- everything needed to generate data, train, optimise, evaluate, run the app and tests.
# Exact versions CI tested against: requirements-lock.txt
numpy>=1.26
pandas>=2.1
pyarrow>=15.0
scikit-learn>=1.4
lightgbm>=4.3
shap>=0.44

# Optimiser
pulp>=2.8
deap>=1.4

# Explainer (the SDK is only called when ANTHROPIC_API_KEY is set)
anthropic>=0.39

# App
streamlit>=1.33

# Utilities
matplotlib>=3.8
pytest>=8.0
